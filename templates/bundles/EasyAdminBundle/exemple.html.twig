<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Scores</title>
    <style>
        /* Add some basic styling for the body */
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 20px;
        }

        /* Style the heading and link */
        h1 {
            margin-bottom: 20px;
        }

        a {
            color: #007bff;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        /* Style the form */
        form {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 10px;
        }

        input[type="text"] {
            padding: 8px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
            width: 100%;
        }

        button[type="submit"] {
            padding: 8px 15px;
            font-size: 16px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        /* Style the student information */
        h2 {
            margin-top: 20px;
            font-size: 24px;
        }

        p {
            margin-bottom: 10px;
        }

        .not-found {
            color: #dc3545;
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><a href="{{ url('dashboard') }}">Retour au menu principal</a></h1>
        <p>Consulter votre score</p>
        <form action="{{ path('app_admin_user_index') }}" method="post">
            <label for="identifier">Entrez votre identifiant :</label>
            <input type="text" id="identifier" name="identifier" required>
            <button type="submit">Rechercher</button>
        </form>

        {% if identifier %}
            <div class="student-info">
                {% if score is not null %}
                    <h2>Informations sur l'étudiant</h2>
                    <p>Identifiant : {{ identifier }}</p>
                    <p>Score : {{ score }}</p>
                {% else %}
                    <p class="not-found">Aucun étudiant trouvé avec l'identifiant fourni.</p>
                {% endif %}
            </div>
        {% endif %}
    </div>
</body>
</html>
